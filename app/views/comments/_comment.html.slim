li.clearfix
  .left
    .avatar.small
      a href="#{user_path(comment.user)}"
        = image_tag comment.user.avatar.url
      .nickname
        a href="#{user_path(comment.user)}" 
          span= comment.user.login
          span.text-muted= comment.created_at
        p= comment.body
  .right
    a.text-muted.reply 回复
    a.ding href="javascript:;" data-votable_type="comments" data-votable_id=comment.id data-vote="up_vote"
      i.kx.kx-up-voted-sm
      small.text-muted= comment.up_votes_count
    / span.text-muted 1楼
  - if comment.hot?
    i.kx.kx-light-up
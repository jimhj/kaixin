var button = '<a class="btn btn-danger btn-xs" data-method="POST" data-remote="true" href="<%= reject_admin_joke_path(@joke) %>">拒绝</a>';

$("tr.joke-item-<%= @joke.id %>").find('.censorship').empty().append(button);